import { Inter, Cinzel } from 'next/font/google';

export const inter = Inter({
    subsets: ['latin'],
    display: 'swap',
  })
   
export const cinzel = Cinzel({
  // weight: ['400'],
  subsets: ['latin'],
  display: 'swap',
})